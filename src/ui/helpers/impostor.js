export const handleImpostorPlacement = (selectedCard, targetCard, impostorRealm, side, isDreamer) => {
    // TODO: Implement impostor placement
    console.log('Placing impostor:', {
        selectedCard,
        targetCard,
        impostorRealm,
        side,
        isDreamer
    });
};
